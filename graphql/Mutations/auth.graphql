extend type Mutation {

     login(identifier: String!, password: String!, device: String!): LoginResponse!

}


type LoginResponse {
    success: Boolean!
    message: String
    token: String
}


extend type Query{
    me: User! @auth @guard
}